# 🚀 Pipeline CI avec Flask

## 🐍 Langage et Framework
- **Langage :** Python 3.14
- **Framework :** Flask

## ⚙️ Étapes de la pipeline
1️⃣ Installation des dépendances (`pip install -r requirements.txt`)  
2️⃣ Démarrage du serveur Flask (`python app.py &`)  
3️⃣ Exécution d’un `curl` pour vérifier la réponse HTTP  
4️⃣ Lancement des tests automatiques (`pytest`)

## 🧪 Lancer le projet manuellement
### 1. Cloner le dépôt
```bash
git clone https://github.com/ton-compte/mon-projet-flask.git
cd mon-projet-flask
